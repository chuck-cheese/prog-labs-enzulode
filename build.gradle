plugins {
    id 'java'
    id 'application'
}

sourceCompatibility = javaVersion
targetCompatibility = javaVersion

dependencies {
    implementation project(':natives')
}

application {
    applicationDefaultJvmArgs = ["-Djava.library.path=./natives/bin"]
    mainClass = 'com.enzulode.lab.Lab'
}